package ooad.gameobserver.intf;

public enum EventType {
    TREASURE_FOUND,
    FIGHT_OCCURRED,
    CHARACTER_DIED,
    CHARACTER_MOVED,
    CHARACTER_ATE,
    CHARACTER_DRANK,
    ADD_CHARACTER,
    GAME_OVER, ALL
}
